package domain;

// Represents the different roles within a team. The enum is easy to use since there are a limited number of roles within a team which serve as 'categories'
public enum TeamRole {
    TeamLead, Bioinformatician, TechnicalSupport, Unknown
}
